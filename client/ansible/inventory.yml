---
# Single-host inventory. Connection details (HostName, Port, User,
# IdentityFile, LocalForward, Compression) live in /root/.ssh/config --
# generated at startup from environment variables -- so Ansible just
# needs to know the host alias.
all:
  hosts:
    ansible-target:
